public class SortedNumber{


    public static double[] getSortedArray(double numOne, double numTwo, double numThree){

        double[] sorted = getSorted(numOne,numTwo,numThree);
        System.out.println(sorted[0] +"  " + sorted[1]+ " " + sorted[2]);
    }

    public static double[] getSortedArray(double numOne, double numTwo, double numThree){
        
        double[] nums= {numOne,numTwo,numThree};
        
        
        for(int index = 0; index <;nums.length - 1; index++ ){
            for(int count = 0)


        }
    }

}
